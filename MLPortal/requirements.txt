# Core
streamlit>=1.35.0
python-dotenv>=1.0.0
pandas>=2.1.0
numpy>=1.26.0
scipy>=1.12.0
pydantic>=2.5.0
joblib>=1.3.0
pyarrow>=15.0.0
openpyxl>=3.1.0

# Visualization
plotly>=5.20.0

# Machine learning — sklearn
# NOTE: RF pipeline was serialised with 1.8.0 — pin to avoid InconsistentVersionWarning on load
scikit-learn==1.8.0

# Gradient boosting libraries (install all for full model registry)
xgboost>=2.0.0
catboost>=1.2.0
lightgbm>=4.3.0

# Explainable ML
interpret>=0.6.0

# Hyperparameter optimisation
optuna>=3.6.0

# Categorical encoding
category-encoders>=2.6.0

# SHAP (model explanation)
shap>=0.45.0

# Target encoding alternative
# category_encoders already included above

# Optional — install manually for UMAP missingness visualisation:
# umap-learn>=0.5.6
# (requires: numba>=0.57.0)

# Optional — SMOTE for class imbalance:
# imbalanced-learn>=0.12.0

# Azure ML + Azure Identity (Stage 9 — Clinical Insight)
azure-ai-ml>=1.15.0
azure-identity>=1.16.0

# Azure OpenAI / GPT-4o via AI Foundry (Stage 9 — Clinical Insight)
openai>=1.30.0

# Deep Learning — local EfficientNet-B4 inference (Stage 9 — Clinical Insight)
# Install torch CPU-only to keep the venv lean (no CUDA drivers needed locally):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install opencv from PyPI:
#   pip install opencv-python-headless
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.7.0
